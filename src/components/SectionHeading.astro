---
const { kicker, title, description, inverse = false } = Astro.props;
---

<div class:list={["section-heading", inverse && "section-heading--inverse"]}>
  <p>{kicker}</p>
  <h2>{title}</h2>
  {description && <div class="section-heading__description">{description}</div>}
</div>
